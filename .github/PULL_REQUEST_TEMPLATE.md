## What & why

<!-- What does this change do, and why? Link any related issue. -->

## How I tested

<!-- e.g. built with ./scripts/build.sh and ran --test-all; manually verified X -->

## Checklist

- [ ] `./scripts/build.sh` succeeds and `--test-all` passes
- [ ] User-facing strings go through `L()` and are added to **both** `en.lproj` and `ru.lproj`
- [ ] Docs updated if behavior changed
- [ ] Change is focused (one concern)
